# kube-escalate
Kubernetes operator and kubectl plugin for Just-in-Time privilege escalation. Grant temporary, time-limited cluster access with automatic expiry, full audit trail via Kubernetes Events, and Prometheus metrics — no database, no external dependencies.
